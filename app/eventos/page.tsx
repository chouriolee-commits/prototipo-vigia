import { EventosContent } from '@/components/eventos-content'

export const metadata = {
  title: 'Eventos — VIGÍA',
  description: 'Historial completo de detecciones registradas por VIGÍA.',
}

export default function EventosPage() {
  return <EventosContent />
}