import { AnimalesContent } from '@/components/animales-content'

export const metadata = {
  title: 'Animales — VIGÍA',
  description: 'Inventario de animales registrados en el sistema.',
}

export default function AnimalesPage() {
  return <AnimalesContent />
}